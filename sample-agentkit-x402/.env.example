# エージェントウォレットの秘密鍵（省略するとランダム生成）
# 本番環境では必ず設定すること
AGENT_PRIVATE_KEY=

# サーバーポート（デフォルト: 3001）
PORT=3001

# ローカル開発用: AgentBook（World ID オンチェーン検証）をスキップ
# 本番環境では必ず false か削除すること
# 本番利用前に登録が必要: npx @worldcoin/agentkit-cli register <address>
SKIP_AGENT_BOOK=true
